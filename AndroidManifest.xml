<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="directi.androidteam.training.chatclient"
          android:versionCode="1"
          android:versionName="1.0"
          android:exported="true" >
    <uses-sdk android:minSdkVersion="16"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name="directi.androidteam.training.chatclient.Authentication.LoginActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="directi.androidteam.training.chatclient.Authentication.DisplayRosterActivity"
                  android:label="@string/app_name">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="directi.androidteam.training.chatclient.Authentication.LoginActivity" />
        </activity>
        <activity android:name="directi.androidteam.training.chatclient.Authentication.LoginErrorActivity"
                  android:label="@string/app_name">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="directi.androidteam.training.chatclient.Authentication.LoginActivity" />
        </activity>
        <activity android:name="directi.androidteam.training.chatclient.activitytest"
                  android:label="@string/app_name">
<!--            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>     -->
        </activity>
        <service android:name=".testtask" />
    </application>
</manifest>